#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    char A[5];
    
    
    /*printf("Enter size of array: ");
    scanf("%d", &n);
    char *A = (char *)malloc(n* sizeof(char));*/
    for (int i = 0; i < 5; i++)
    {
        printf("%d\n", A[i]);
    }
}