#include <stdlib.h>
#include <stdio.h>

void main()
{
    // int a[5];
    // int i;
    // a[5] = 4;
    // printf("%ld\n", sizeof(a));
    // int *a;
    // // int i;
    // a = (int *)malloc(sizeof(int));
    // printf("0x%ld\n", sizeof(a));

    // for (int i = 0; i < 20; i++)
    // {
    //     a[i] = i;
    //     printf("a[%i] : %i\n", i, a[i]);
    // }
    printf("%d", *(37779de0));
}